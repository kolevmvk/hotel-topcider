/** Supabase adapter stub — aktivira se kada je ASSISTANT_BACKEND=supabase i šema deploy-ovana. */

import type { AssistantStoreAdapter } from "../types";

export function getSupabaseAssistantAdapter(): AssistantStoreAdapter | null {
  return null;
}
